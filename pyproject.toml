[project]
name = "unbranding-llm"
version = "0.1.0"
description = "Evaluation harness for brand unlearning in LLMs"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.7",
    "pyyaml>=6.0",
]
