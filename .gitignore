# Python
__pycache__
.venv
venv

# Container images live outside the repo, on shared NFS (containers/README.md)
*.sif

# Produced at run time
runs/
judged/
logs/*
!logs/.gitkeep
scores.csv
compare.csv
dist/

# Weights and caches never belong in git
models/
models_unlearned/
.cache/

# Local review scratch, not part of the repo
dataset/eval/DUPLICATES_REVIEW.md
config.trade_dress.proposal.yaml
